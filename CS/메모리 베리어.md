- **리드 배리어:** 내 **인밸리데이션 큐**를 청소해서, 내가 옛날 값을 읽는 실수를 막는다. (수신자 부담 청소)
    
- **라이트 배리어:** 내 **스토어 버퍼**의 순서를 고정하고 L1 캐시로 밀어내서, 남들이 내가 쓴 값들의 순서를 오해하지 않게 만든다. (송신자 책임 배달)

> 라이트 배리어는 하나의 변수의 스레드 경합안전을 보장하지 않음 (서로 다른 스레드가 둘다 x를 쓰기 버퍼에 넣는걸 막지않음)